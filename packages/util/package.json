{
  "name": "rubrc-util",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "clean": "tsc --build --clean && rimraf dist node_modules",
    "build": "tsc --build --verbose"
  },
  "devDependencies": {
    "better-typescript-lib": "*",
    "rimraf": "*",
    "typescript": "*"
  }
}
